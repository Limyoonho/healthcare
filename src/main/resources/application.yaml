spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:9999/healthcare?useSSL=false&characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: root
    password: dladbsgh1@
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
  config:
    location: project.healthcare.config

server:
  port: 8080