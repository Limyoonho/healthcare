spring:
#  datasource:
#    url: jdbc:mysql://127.0.0.1:3306/boda?useSSL=false&characterEncoding=UTF-8&serverTimezone=Asia/Seoul
#    username: root
#    password: dladbsgh1@
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
  config:
    location: project.healthcare.config

server:
  port: 8080